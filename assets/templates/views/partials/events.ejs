<script type="text/ng-template" id="views/partials/events.html">
<div>
  <h2>Events</h2>
  <p> There are {{events.length}} events</p>
  <ul>
    <li ng-repeat="event in events">
      {{event.title}} - {{event._resource.name}}
    </li>
  </ul>
</div>
</script>
